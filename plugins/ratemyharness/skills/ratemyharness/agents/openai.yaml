interface:
  display_name: "RateMyHarness"
  short_description: "Evidence-based audits for agent harnesses"
  default_prompt: "Use $ratemyharness to audit this agent harness, find release blockers, and give me the three fastest fixes."
policy:
  allow_implicit_invocation: true
